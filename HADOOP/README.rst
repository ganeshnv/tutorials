Hadoop installation:
====================

Update the hosts
================


Ansible Play-book command:
==========================

` # ansible-playbook -i hosts hadoop.yml --diff --vault-ask-password -vv `
